server.port=${PORT:8080}
spring.application.name=frameforge

# ── Multipart ──
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=100MB
spring.servlet.multipart.max-request-size=500MB

# ── FFmpeg ──
ffmpeg.path=ffmpeg

# ── Database ──
spring.datasource.url=${JDBC_DATABASE_URL:jdbc:h2:file:./frameforge-db;DB_CLOSE_ON_EXIT=FALSE}
spring.datasource.username=${JDBC_DATABASE_USERNAME:sa}
spring.datasource.password=${JDBC_DATABASE_PASSWORD:}
spring.datasource.driver-class-name=${DB_DRIVER:org.h2.Driver}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.database-platform=${JPA_DIALECT:org.hibernate.dialect.H2Dialect}

# H2 console (local dev only)
spring.h2.console.enabled=${H2_CONSOLE:true}

# ── JWT ──
jwt.secret=${JWT_SECRET:FrameForge-Change-This-Secret-In-Production-Min32Chars!}
jwt.expiration-ms=86400000

# ── CORS ──
cors.allowed-origins=${CORS_ORIGINS:http://localhost:3000}

# ── Logging ──
logging.level.com.frameforge=DEBUG
```

---

## Step 3 — Set these 4 env vars in Render RIGHT NOW

Go to **Render → frameforge-backend → Environment** → Add each one:

| Key | Value |
|---|---|
| `JDBC_DATABASE_URL` | `jdbc:postgresql://dpg-d75201m3jp1c739bm940-a/frameforge_db` |
| `JDBC_DATABASE_USERNAME` | `frameforge_db_user` |
| `JDBC_DATABASE_PASSWORD` | `mrqw7vER3XA7p9dhD9DIfiTUBwNwSZZN` |
| `DB_DRIVER` | `org.postgresql.Driver` |
| `JPA_DIALECT` | `org.hibernate.dialect.PostgreSQLDialect` |
| `H2_CONSOLE` | `false` |

> The hostname `dpg-d75201m3jp1c739bm940-a` came from your first error log. Confirm it matches what's in your Render PostgreSQL → Connect tab.

Click **"Save Changes"** after adding all vars — Render will auto-redeploy.

---

## Step 4 — Verify on Render

After saving env vars, watch the logs. You should see:
```
HikariPool-1 - Starting...
HikariPool-1 - Start completed.
Started FrameForgeApplication in X seconds

